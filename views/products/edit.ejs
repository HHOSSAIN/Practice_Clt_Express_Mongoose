<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit the Product:</h1>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="names">Product name</label>
        <input type="text" id="names" placeholder="type the item name" name="name" value="<%= product.name %> ">

        <!--input type="text" placeholder="type the category" name="category"-->
        <h3>The category dropdown</h3>
        <label for="categories">Product category</label>
        <select name="category" id="categories">
            <%// for (let category of categories){ %> 
                <!--option value="<%=// category %> " <%=// product.category === category ? 'selected' : '' %> > <%=// category %>  </option-->
            <%// } %> 
            <option value="fruit" <%= product.category === 'fruit' ? 'selected' : '' %> >fruits</option>
            <option value="vegetable" <%= product.category === 'vegetable' ? 'selected' : '' %> >vegetable</option>
            <option value="dairy" <%= product.category === 'dairy' ? 'selected' : '' %> >dairy</option>
        </select>

        <!--label for="prices">Product price</label-->
        <!--input type="number" id="prices" placeholder="type the price" name="price"-->
        <label for="prices">Product price</label>
        <input type="number" id="prices" placeholder="type the price" name="price" value="<%= product.price %>">
        <button>Submit</button>
    </form>    
</body>
</html>